import scala.collection.mutable.ListBuffer
import scala.concurrent.{ExecutionContext, Future}
import scala.util.control.NonFatal

object Launcher extends App {

  var chahrs = Array('a')
  val res =
    //"test".getChars(0,1,chahrs, 0)
    'l'.sign
  println(s"res: $res")

}
